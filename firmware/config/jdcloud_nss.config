# NSS-accelerated QoS (the qosmio tree's flagship; packages come from its bundled sqm_scripts_nss feed)
# NSS core drivers (nss-drv/ecm/dp/bridge-mgr) are target defaults of that tree, no need to select them
CONFIG_PACKAGE_sqm-scripts-nss=y
CONFIG_PACKAGE_luci-app-sqm=y
